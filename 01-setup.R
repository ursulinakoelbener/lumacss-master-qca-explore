# Install the QCA package from the Comprehensive R Archive Network (CRAN)
install.packages("QCA")
install.packages("admisc")

# Load the QCA package into the R session
library(QCA)

# start graphical user interface
runGUI()
